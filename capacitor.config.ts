import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'folhasObra2022',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
